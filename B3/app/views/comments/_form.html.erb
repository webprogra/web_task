<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>
    <%= hidden_field_tag(:allowed,1) %>
  <p>
    <%= f.submit %>
  </p>
<% end %>
