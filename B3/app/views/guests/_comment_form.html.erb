<%= form_for([@post, @post.comments.build]) do |f| %> 
  <p>
    <%= f.label:nickname %><br/>
    <%= f.text_area :nickname %>
  </p>
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
